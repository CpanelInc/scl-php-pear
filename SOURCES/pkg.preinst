#!/bin/bash

# vars set when slurped into maintenance scripts it autogenerated maintenance scripts
# no mechanism for that in SPEC yet, also looks in debian/ and not <macro>.debian/
# we can not yet use them in the spec until ZC-8893 does ^^^

# Manage relocation of metadata, before update to pear
if [ -d $peardir/.registry -a ! -d $metadir/.registry ]; then
  mkdir -p $metadir
  mv -f $peardir/.??* $metadir
fi
